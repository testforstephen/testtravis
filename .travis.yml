language: cpp

os:
  - linux
  - osx

before_install:
  - git clone https://github.com/creationix/nvm.git ./.nvm
  - source ./.nvm/nvm.sh
  - nvm install 6.9.4
  - nvm use 6.9.4
  - npm config set python `which python`
  - npm install -g vsce
  - npm install -g typescript
  - if [ $TRAVIS_OS_NAME == "linux" ]; then
      export CXX="g++-4.9" CC="gcc-4.9" DISPLAY=:99.0;
      sh -e /etc/init.d/xvfb start;
      sleep 3;
    fi

install:
  - npm install
  - npm run vscode:prepublish

script:
  - npm test --silent

# safelist
branches:
  only:
  - master
  - /v?\d+\.\d+(\.\d+)?/

deploy:
  provider: script
  script: scripts/vsce_publish.sh
  skip_cleanup: true
  on:
    tags: true
    all_branches: true
